server {
    listen 80;

    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Redirecționare pentru rutele client-side
    location / {
        try_files $uri /index.html;
    }
}
